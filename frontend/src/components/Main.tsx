import { Flex } from '@chakra-ui/react';

const Main = () => (
  <Flex
    justifyContent="center"
    alignItems="center"
    flexDirection="column"
    h="100%"
  >
    Main 변경된 파일 반영 빌드 확인
  </Flex>
);

export default Main;
